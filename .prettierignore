dist
coverage
node_modules
playwright-report
test-results
android
public
package-lock.json
supabase/.temp

# The approved architecture documents. They are the specification this codebase
# implements; reformatting them would produce a diff that says nothing and
# obscures the ones that do.
docs

# Generated from supabase/seed.sql by scripts/generate-category-constants.mjs.
# The generator owns its formatting; `gen:categories --check` is what verifies it.
src/config/categories.generated.ts
